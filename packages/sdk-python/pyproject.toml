[project]
name = "faccp-sdk"
version = "0.1.0"
description = "Official Python SDK for FACCP Platform APIs"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
