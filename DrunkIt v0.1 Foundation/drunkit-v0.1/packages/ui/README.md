# @drunkit/ui

Shared DrunkIt design-system package.

Initial components should remain minimal and accessibility-first. Consumer, retailer and admin applications should consume shared primitives rather than duplicate visual systems.
