[project]
name = "faccp-services"
version = "0.1.0"
description = "FACCP Python Backend Services"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.3.0",
    "sqlalchemy[asyncio]>=2.0.30",
    "asyncpg>=0.29.0",
    "redis>=5.0.4",
    "pyjwt[crypto]>=2.8.0",
    "passlib[bcrypt]>=1.7.4",
    "python-multipart>=0.0.9",
    "httpx>=0.27.0",
    "structlog>=24.2.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "python-jose[cryptography]>=3.3.0",
    "aiokafka>=0.10.0",
    "prometheus-client>=0.20.0",
    "alembic>=1.13.0",
    "numpy>=1.26.0",
    "PyYAML>=6.0",
    "aiosqlite>=0.20.0",
    "email-validator>=2.0",
    "opentelemetry-api>=1.24.0",
    "opentelemetry-sdk>=1.24.0",
    "opentelemetry-exporter-otlp>=1.24.0",
    "opentelemetry-instrumentation-fastapi>=0.45b0",
    "opentelemetry-instrumentation-sqlalchemy>=0.45b0",
    "jsonschema>=4.20.0",
    "slowapi>=0.1.9",
    "psycopg2-binary>=2.9.9",
    "hypothesis>=6.100.0",
    "pytest-cov>=5.0.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# Explicitly list importable packages so hatchling can resolve the wheel.
# This monorepo has no top-level Python package named 'faccp_services';
# the shared runtime library lives in services/_common/faccp_common.
packages = ["services/_common/faccp_common"]

[tool.uv.workspace]
members = ["services/*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests/unit"]

[tool.ruff]
line-length = 100
target-version = "py311"
