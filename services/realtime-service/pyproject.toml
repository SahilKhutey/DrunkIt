[project]
name = "faccp-realtime"
version = "0.1.0"
description = "FACCP Realtime WebSocket & SSE Live Broadcast Service"
requires-python = ">=3.11"
dependencies = [
    "faccp-common",
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "websockets>=12.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.2.0",
    "redis[hiredis]>=5.0.4",
    "aiokafka>=0.10.0",
    "structlog>=24.1.0",
    "prometheus-client>=0.20.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
