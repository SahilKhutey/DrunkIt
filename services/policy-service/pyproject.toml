[project]
name = "faccp-policy-service"
version = "0.1.0"
description = "FACCP Master ABAC Policy Engine, SoD Conflict Detector & Break-Glass Service"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
    "httpx>=0.27.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
